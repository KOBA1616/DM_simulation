Untranslated UI candidates report

Search scope: dm_toolkit/**
Generated: 2026-03-08

Summary: Collected occurrences of common UI setter calls. Review each entry and decide whether to wrap the literal with tr(...) and add/confirm a key in data/locale/ja.json.

--- setText occurrences (sample; full list in editor) ---
Refer to occurrences previously found by search (many matches). Key examples:
- dm_toolkit/gui/simulation_dialog.py: lines setting wins/losses/draws/status_label — use tr('Wins')/tr('Losses')/tr('Draws') already present in many places; verify translation entries exist.
- dm_toolkit/gui/editor/preview_pane.py: many fields use data.get('name', '???') etc. Consider tr('???') or safer: keep fallback but ensure display always via tr() when static.
- dm_toolkit/gui/editor/preview_pane.py: tp_spell_name_label uses tr(spell_data.get('name', '呪文')) already.

--- setWindowTitle occurrences ---
- dm_toolkit/gui/deck_builder.py: self.setWindowTitle(tr("Deck Builder"))
- dm_toolkit/gui/app.py: setWindowTitle(tr("DM AI Simulator"))
- dm_toolkit/gui/dialogs/setup_config_dialog.py: self.setWindowTitle(tr("Setup Config"))
- dm_toolkit/gui/editor/window.py: self.setWindowTitle(tr("Card Editor Ver 2.0"))
(others already use tr() or pass title variable)

--- setTitle occurrences (group boxes etc.) ---
- dm_toolkit/gui/editor/forms/modifier_form.py: several setTitle calls use raw Japanese strings like "軽減対象カード" (not wrapped in tr()).
- dm_toolkit/gui/editor/forms/parts/filter_widget.py: self.basic_group.setTitle(title) — title may be tr()-wrapped elsewhere.

--- setPlaceholderText occurrences ---
- Many placeholders already use tr(...). A few are schema-driven and may require adding to schema tooltip translations.

--- setToolTip occurrences ---
- Most tooltips already use tr(...); some raw Japanese/English literals remain (e.g., keyword_form.py rev_change_check tooltip).

--- Next steps suggested ---
1) Review `reports/untranslated_ui_candidates.txt` and mark entries to auto-change or skip.
2) If approved, apply automated wrapping for static literals (e.g., setTitle("軽減対象カード") -> setTitle(tr("軽減対象カード")).
3) Add missing keys to `data/locale/ja.json` for newly wrapped strings.
4) Run full test suite and quick UI smoke to validate.

Note: This report is intentionally conservative — dynamic values and empty strings are excluded from automatic wrapping to avoid runtime issues.
