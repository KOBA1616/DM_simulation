```markdown
Summary
-------
Fix indentation and pytest conversion for `tests/test_phase4_e2e.py` and ensure `tests/test_action_to_command.py` expectations align with Phase4 normalization.

... (moved from repository root `PR_NOTES.md`)

```
