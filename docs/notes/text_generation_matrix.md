```markdown
# テキスト生成マトリクス

以下は `dm_toolkit.gui.editor.text_generator.CardTextGenerator` の `ACTION_MAP` と
`_format_action` / `_format_command` に基づく、アクション型 → 生成されるテキスト関係の網羅表です。

... (元の `scripts/text_generation_matrix.md` の内容を移動)

```
