# Recurrence prevention: generated build outputs must stay outside Git and Git LFS.
# Keep this file free of build-output LFS rules so ignored build trees cannot silently re-enter history.

# Uncomment only if you intentionally want long-lived model artifacts in LFS.
# *.pth filter=lfs diff=lfs merge=lfs -text
