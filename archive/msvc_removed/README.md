MSVC artifacts removed from repository

This folder contains a record that Visual Studio / MSVC generated files were removed
from the repository on 2025-12-31 to standardize on CMake/Ninja builds.

If you need to recover them, regenerate locally with CMake (don't commit generated files).
