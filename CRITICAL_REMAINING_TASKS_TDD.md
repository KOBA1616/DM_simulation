# 重要残タスクと改善計画（未完了のみ）

最終更新: 2026-03-13

この文書は未完了タスクのみを管理します。
完了した作業ログ・履歴は本ファイルから削除しました。

---

## 0. 現状サマリ

- ネイティブ拡張: `dm_ai_module.cp312-win_amd64.pyd`
- 直近テスト状況: `pytest tests/ -q --tb=short` で `297 passed, 1 skipped`
- 既存TDDタスク（T-01/T-04/T-05/T-06/T-07/T-08/T-09）は、文書内では完了扱いだったため本ファイルから除外

---

## 1. 残タスク（実施対象）

現在、未完了タスクはありません。

---

## 2. 再発防止メモ（運用）

- 完了済みタスクは本ファイルから削除し、必要なら `reports/` または専用ログへ移管する。
- このファイルには「未完了タスク + 完了条件」だけを残し、進捗履歴の肥大化を防ぐ。
- タスク更新時は、完了条件のチェック状態と検証コマンドを必ず同期する。
